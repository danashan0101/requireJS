define('Dom', ['Geometry'], function (Geometry) {
    return 'Utilities for Dom (depend on "' + Geometry + '")';
});

define('Geometry',function () {
    return 'Utilities for Geometry';
});

define('Array', function () {
    return 'Utilities for Array';
});